To deploy to Google App Engine, add valid oauth + app-engine details to appengine-web.xml (see appengine-web.dummy.xml), then from project base run:
lein clean && lein ring uberwar && rm -rf gae && unzip -d gae target/*.war && cp appengine-web.xml gae/WEB-INF && $APPENGINE_HOME/bin/appcfg.sh update gae
